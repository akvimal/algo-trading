fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
sqlalchemy==2.0.30
psycopg[binary]==3.1.19
apscheduler==3.10.4
requests==2.32.3
tzdata==2024.1
websocket-client==1.8.0
PyJWT==2.8.0
# Shared quote cache + pub/sub fan-out for the live LTP push layer
# (app/providers/dhan_feed.py's _publish_tick, app/api/routes/quotes_ws.py)
# - this service's first-ever Redis dependency, otherwise in-memory-cache-
# only by design (see its README). Same pin execution/signal-engine
# already use.
redis==5.0.4
# Long-history (>90 day) daily/weekly OHLCV for NSE equities/indices, where
# Dhan's own history endpoint is capped - see app/providers/yahoo.py.
yfinance==1.7.0
# Already an implicit yfinance dependency (its own DataFrame return type) -
# pinned explicitly now that yahoo.py's own retry helper imports it
# directly (pd.DataFrame()), not just consuming what yfinance hands back.
pandas==3.0.5
