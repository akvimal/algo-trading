# Minimal dev config for the orders.resolved stream between
# signal-processing and execution. Revisit before any non-local deployment
# (set a requirepass, bind to a private interface, enable TLS).

appendonly yes
appendfsync everysec
maxmemory 256mb
maxmemory-policy noeviction
