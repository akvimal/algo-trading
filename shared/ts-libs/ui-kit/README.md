# ui-kit (not yet used)

Placeholder for React components/design tokens shared across the
per-system frontends, once there's a second frontend to justify it
(currently only `signal-processing/frontend` exists).
